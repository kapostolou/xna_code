See the Effect structure pdf file in the Diagrams/Effect _Structure directory for a description of these classes.

The Force_Quad_Instance_Manager is actually just an alias for the instantiation of a generic instancing template.

The Helper Directory contains a helper class used by the Spring_Effect to calculate addresses when setting up surfaces